The files in this project are intended to be utilized for visualization of matrix decomposition.
In order to utilize this file you must enter the virtual environment; 
Do so by cd'ing to the root directory "/path/to/MA490"
Then type "./scripts/activate.bat"; 
After this you should see in your command shell "(venv) ..."

If you want to deactivate the venv jsut type "deactivate"

